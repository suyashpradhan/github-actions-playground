name: GitHub Actions
run-name: Basic CI/CD ran by ${{ github.actor }}
on:
  - push
jobs:
  Job-1:
    runs-on: ubuntu-latest
    steps:
      - run:
          echo "🎉 The job was automatically triggered by a ${{ github.event_name }}
          event."
      - run: echo "🎉 The job was triggered by a ${{ github.actor }} user."
  Job-2:
    runs-on: ubuntu-latest
    steps:
      - run:
          echo "🎉 The job was automatically triggered by a ${{ github.event_name }}
          event."
  Job-3:
    runs-on: ubuntu-latest
    steps:
      - name: Install Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'
      - run: npm install axios
      - run: echo "Axios ${{axios -v}} was installed successfully, now you can use it in your workflow."
